export enum Method {
  GET = 'GET',
  DELETE = 'DELETE',
  POST = 'POST',
  PUT = 'PUT',
}

export enum EntityType {
  ATTRIBUTES = 'attributes',
  GROUP = 'group',
  SESSION = 'session',
  USER = 'user'
}

export enum QueryTypes {
  GROUPS = 'groups',
  USERS = 'users'
}